filetype plugin indent on
" On pressing tab, insert spaces
set expandtab

" show existing tab with 4 spaces width
set tabstop=4

" > indents with tabstop setting
set shiftwidth=4

" set tabstop=2 softtabstop=0 expandtab shiftwidth=2 smarttab
